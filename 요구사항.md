# 요구사항

### 백엔드(BE) 및 인프라 핵심 기억 포인트

### 1. 비즈니스 로직 및 API 확장

- **호스트 기능:** 숙소 등록 및 본인 숙소 관리 API
- **관리자 기능:** 관리자 페이지 전용 API
- **실시간/고급 기능:** 채팅 또는 알림(실시간), 트래픽 집중 대비 **대기 시스템** 기능 구현
- **외부 연동:** 구글 지도 API 등 외부 서비스 연동

### 2. 기술 스택 및 코드 구현 원칙

- **Core:** Spring Boot 3.X + JPA + QueryDSL 조합
- **인증:** Spring Security 기반 **GitHub 또는 Google OAuth** 로그인 구현 (FE의 로그인 유지 흐름과 연동)
- **테스트:** 가급적 **BDD(Behavior-Driven Development)** 방식으로 테스트 코드 작성 (`Given/When/Then`)
- **문서화:** ERD를 설계하고 README에 필수로 포함 (기존에 논의했던 surrogate key 및 `deleted_at` 활용 전략 유지 가능)

### 3. 배포 및 인프라 아키텍처

- **서버/DB:** AWS EC2 (small 인스턴스) + RDS (t3.micro, 단일 인스턴스)
- **웹 서버:** **Nginx**를 활용한 80포트 역방향 프록시(Reverse Proxy) 및 **HTTPS(SSL)** 적용
- **CI/CD:** Docker와 GitHub Actions를 활용한 배포 자동화
- **AWS 확장 클라우드:** 요구사항에 맞춰 ELB, S3, CodeDeploy, Lambda, DynamoDB 등 다양한 AWS 생태계 적극 활용
- **모니터링:** 시스템 안정성을 위한 모니터링 및 부하 테스트 도구 도입 및 학습

### 프론트엔드(FE) 및 인프라 핵심 기억 포인트

 **핵심 요구사항 요약 및 기억 포인트**
• **기술 스택:** React + Vite, JavaScript 우선 (TypeScript 선택), React Router, Fetch API, 환경 변수(`VITE_API_BASE_URL`) 관리.
• **구현 프로세스:** 1.  `mock data`를 활용한 5개 필수 정적 화면(메인 목록, 상세, 로그인, 예약 확인, 마이페이지) 구현
2.  API 호출 코드 분리 관리 및 로딩/성공/실패/데이터 없음 상태 화면 처리
3.  일반 사용자 흐름(검색 $\rightarrow$ 상세 조회 $\rightarrow$ 로그인 후 예약 및 취소) 구현
4.  호스트 흐름(숙소 등록 폼 검증 및 관리) 구현
• **AI Agent 협업 원칙:** 작업을 작고 구체적인 단위로 나누어 요청하고, 생성된 코드를 팀원이 완벽히 이해 및 설명할 수 있도록 검증할 것.
• **예외 처리:** 401(미인증), 403(권한 없음) 응답 구분 및 POST/PUT/DELETE 후 데이터 갱신(Re-fetching).

---

---

---

---

## 도메인 중심 정리:

### 1. 인증 및 권한 도메인 (IAM - Identity & Access Management)

사용자의 신원을 확인하고 역할(게스트, 호스트, 관리자)에 따른 접근 권한을 제어하는 영역입니다.

- **핵심 기능:** Google/GitHub OAuth 로그인, 사용자 정보 조회(마이페이지), 로그아웃, 역할 기반 라우팅.
- **프론트엔드:** 로그인 상태 관리(Context API 권장), 인증 만료 및 401/403 에러 리다이렉트 처리.
- **백엔드:** Spring Security를 활용한 OAuth2 통합, 세션 또는 JWT 토큰 발급 및 검증.

### 2. 카탈로그 및 숙소 관리 도메인 (Catalog & Property)

숙소 데이터의 생명주기를 관리하고, 사용자가 원하는 조건의 숙소를 탐색할 수 있도록 지원하는 영역입니다.

- **핵심 기능:** 숙소 등록/수정/비활성화(호스트), 조건부 검색(지역, 날짜, 인원), 지도 기반 탐색, 상세 정보 조회.
- **프론트엔드:** 검색 바(SearchBar), 숙소 카드(RoomCard), S3 또는 외부 URL을 이용한 이미지 업로드 폼, 입력값 유효성 검사.
- **백엔드:** Spring Boot와 JPA를 이용한 도메인 모델링. QueryDSL을 활용한 동적 검색 쿼리 작성.

### 3. 예약 및 거래 도메인 (Reservation & Transaction)

이 서비스의 핵심 비즈니스 로직으로, 예약 생성부터 취소까지의 상태 변화를 다룹니다.

- **핵심 기능:** 예약 가능 여부 확인, 예약 생성, 예약 목록 조회, 예약 취소, 예약 현황 대시보드(관리자).
- **프론트엔드:** 날짜 및 인원 선택에 따른 실시간 예상 결제 금액 계산, 예약 실패 사유(에러 메시지) UI 렌더링.
- **백엔드:** 동시성 제어(Double Booking 방지), 트랜잭션 경계 설정, 예약 상태(대기, 확정, 취소 등) 관리.

### 4. 실시간 통신 도메인 (Real-time Communication)

사용자 경험을 향상시키기 위해 비동기적이고 즉각적인 피드백을 제공하는 부가/확장 영역입니다.

- **핵심 기능:** 호스트-게스트 간 실시간 채팅, 시스템 알림 목록.
- **프론트엔드:** WebSocket 또는 SSE(Server-Sent Events)를 활용한 실시간 이벤트 수신 및 UI 업데이트.
- **백엔드:** 실시간 메시지 브로커 연동, 세션 관리.

### 5. 인프라 및 코어 시스템 도메인 (Infrastructure & Core System)

프레임워크가 제공하는 추상화 벽을 넘어, 실제 시스템의 성능과 안정성을 좌우하는 아키텍처 영역입니다. 특히 대용량 트래픽 처리와 데이터베이스 최적화에 집중할 수 있는 파트입니다.

- **핵심 기능:** 대기 시스템(Waitlist), CI/CD 자동화, 클라우드 리소스 프로비저닝, 부하 테스트 및 모니터링.
- **인프라 구성:** AWS EC2, RDS, ELB, S3 연동. Nginx를 활용한 리버스 프록시 및 HTTPS 설정.
- **백엔드 과제:** 트래픽 폭주 시나리오를 가정한 대기열 로직 구현. 데이터베이스 인덱싱(B-Tree 등)을 고려한 쿼리 튜닝. Docker와 Github Actions를 이용한 파이프라인 구축.